import { styled } from "@mui/material/styles";
import { Stack } from "@mui/material";

const SignUp = () => {
	return (
		<PageWrapper>
        </PageWrapper>
	)
}

const PageWrapper = styled(Stack)({
	display: "flex",

})

export { SignUp };